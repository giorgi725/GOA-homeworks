// 12) შექმენით მასივი რიცხვებით. Math.max და Math.min მეთოდების გამოყენებით იპოვეთ მასივში ყველაზე დიდი და ყველაზე პატარა რიცხვი. კომენტარებით ახსენით რატომ არის საჭირო spread ოპერატორი (...)

let numbers = [10, 5, 20, 3, 50, 8];

// Math.max და Math.min არ იღებს მასივს პირდაპირ,
// აქ საჭიროა spread ოპერატორი (...) რათა გავამრავლებთ მასივის ელემენტებს ცალ-ცალ არგუმენტებად
let maxNumber = Math.max(...numbers);
let minNumber = Math.min(...numbers);